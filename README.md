# extend-hq/homebrew-tap

Homebrew tap for [Extend CLI](https://github.com/extend-hq/extend-cli).

```sh
brew tap extend-hq/tap
brew install extend
```

Formula updates are published automatically on each tagged release of
`extend-hq/extend-cli` — do not edit `Formula/extend.rb` by hand.
